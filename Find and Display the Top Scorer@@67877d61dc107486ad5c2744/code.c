#incclude<stdio.h>
struct student{
    int roll_number;
    char name[50];
    int marks;
};
int main(){
    int n;
    scanf("%d",&n);
    struct student[];
    for(int i=1;i<=n;i++){
        or (i = 0; i < n; i++) {
        printf("Roll Number: %d,", students[i].rollNumber);
        printf(" Name: %s,", students[i].name);
        printf(" Marks: %.2f \n", students[i].marks);
    }
        
    }
}